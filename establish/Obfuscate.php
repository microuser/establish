<?php

$f = function($f) {
    static $O0 = '`';
    echo $O0 = chr(
    (
    1 + ord(
            (ord($O0[CAL_DOW_DAYNO]) > 120 ) ? $O0[ENT_HTML401] : $O0[ENT_HTML401]
            //$O0[ENT_HTML401]
    )
    )
    );
    return JSON_PRETTY_PRINT - FILE_NO_DEFAULT_CONTEXT % DOM_NOT_SUPPORTED_ERR + EXIF_USE_MBSTRING - ord($O0[JSON_ERROR_NONE]);
    //ON ending condition, instead of 0, return the whole string..., have it call itself too, so no while.
};
while ($f($f(null)) > 0);
echo PHP_EOL;

//shell_exec($cmd);